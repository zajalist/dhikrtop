{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Tauri: Dev",
      "type": "node-terminal",
      "request": "launch",
      "command": "npm run app:dev",
      "cwd": "${workspaceFolder}",
      "preLaunchTask": "Kill Dev Servers"
    },
    {
      "name": "Tauri: Dev (Windows Only)",
      "type": "node-terminal",
      "request": "launch",
      "command": "npm run app:dev",
      "cwd": "${workspaceFolder}",
      "preLaunchTask": "Kill Dev Servers"
    }
  ],
  "compounds": [
    {
      "name": "Tauri: Development (Multi-platform)",
      "configurations": ["Tauri: Dev"]
    }
  ]
}
